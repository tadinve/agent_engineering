{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "outreach_message.schema.json",
  "title": "Outreach message (standalone)",
  "description": "Standalone copy of account_brief.schema.json's $defs.outreach_draft, kept in sync deliberately — see tests/ch05's consistency check. Exists as its own file because a later chapter's Message Composer validates one draft at a time, before it's assembled into a full Account Brief.",
  "type": "object",
  "additionalProperties": false,
  "required": ["message_type", "text", "word_count"],
  "properties": {
    "message_type": {
      "type": "string",
      "enum": ["email", "linkedin_connection_note", "meeting_opening_question"]
    },
    "text": { "type": "string", "minLength": 1 },
    "word_count": { "type": "integer", "minimum": 0 }
  }
}
